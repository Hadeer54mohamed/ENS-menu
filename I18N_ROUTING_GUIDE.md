# 🌍 دليل تفعيل نظام اللغات المتعددة (i18n Routing)

## ✅ التغييرات التي تمت

تم تفعيل نظام الـ routing المتعدد اللغات في Next.js بنجاح! الآن يمكنك استخدام:

- `localhost:3000/ar` - للصفحة العربية
- `localhost:3000/en` - للصفحة الإنجليزية
- `localhost:3000` - يتم توجيهه تلقائياً للغة الافتراضية (العربية)

## 📁 الهيكل الجديد

```
app/
├── layout.tsx (Root Layout)
└── [locale]/
    ├── layout.tsx (Locale-specific Layout)
    └── page.tsx (Homepage with locale)
```

## 🔧 الملفات المعدلة

### 1. **middleware.ts** (جديد)
- يكتشف اللغة تلقائياً من المتصفح
- يوجه المستخدم للصفحة المناسبة حسب اللغة
- يدعم `/ar` و `/en`

### 2. **app/[locale]/layout.tsx** (جديد)
- يحدد اللغة والاتجاه (RTL/LTR) بناءً على الـ locale
- يطبق الإعدادات على الـ HTML
- **يستخدم `React.use()` لفتح params Promise (Next.js 15+)**

### 3. **app/[locale]/page.tsx** (جديد)
- الصفحة الرئيسية مع دعم اللغات
- يغير اللغة تلقائياً حسب الـ URL
- **يستخدم `React.use()` لفتح params Promise (Next.js 15+)**

### 4. **src/hooks/useLanguage.ts**
- تم تحديثه ليعمل مع Next.js Router
- يستخدم `useParams` للحصول على اللغة من الـ URL
- يوجه المستخدم للصفحة الصحيحة عند تغيير اللغة
- **متوافق مع Next.js 15+ async params**

### 5. **src/i18n/index.ts**
- تم إضافة دعم الكشف عن اللغة من الـ URL (`path`)
- يتزامن تلقائياً مع الـ URL locale

## 🆕 تحديث مهم (Next.js 15+)

في Next.js 15 والإصدارات الأحدث، أصبح `params` عبارة عن **Promise** يجب فتحه باستخدام `React.use()`:

```typescript
// ❌ الطريقة القديمة (لا تعمل في Next.js 15+)
export default function Page({ params }: { params: { locale: string } }) {
  const locale = params.locale;
}

// ✅ الطريقة الصحيحة (Next.js 15+)
import { use } from 'react';

export default function Page({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = use(params);
}
```

## 🎯 كيفية الاستخدام

### تغيير اللغة عبر الكود:
```typescript
import { useLanguage } from '@/hooks/useLanguage';

const { toggleLanguage, setLanguage } = useLanguage();

// Toggle between ar/en
toggleLanguage();

// Set specific language
setLanguage('en');
```

### تغيير اللغة عبر الـ URL:
- اذهب إلى `localhost:3000/ar` للعربية
- اذهب إلى `localhost:3000/en` للإنجليزية

### زر تبديل اللغة:
- الموجود في Navbar يعمل تلقائياً
- يوجه المستخدم للصفحة المناسبة

## 🚀 المميزات

✅ **SEO Friendly** - كل لغة لها URL خاص
✅ **Auto Detection** - يكتشف لغة المتصفح تلقائياً
✅ **Shareable Links** - يمكن مشاركة الروابط بلغة محددة
✅ **RTL/LTR Support** - يطبق الاتجاه الصحيح تلقائياً
✅ **No Hydration Issues** - تم حل مشاكل الـ hydration
✅ **Next.js 15+ Compatible** - متوافق مع async params

## 🧪 للاختبار

1. شغل المشروع:
   ```bash
   npm run dev
   ```

2. جرب الروابط:
   - `http://localhost:3000` → يوجهك لـ `/ar`
   - `http://localhost:3000/ar` → صفحة عربية
   - `http://localhost:3000/en` → صفحة إنجليزية

3. جرب زر تبديل اللغة في Navbar

## 📝 ملاحظات

- اللغة الافتراضية: العربية (ar)
- اللغات المدعومة: العربية (ar)، الإنجليزية (en)
- يتم حفظ اللغة في localStorage تلقائياً
- يتزامن localStorage مع URL
- متوافق مع Next.js 16.1.0 (Turbopack)

## 🔄 كيفية إضافة لغة جديدة

1. أضف ملف الترجمة في `src/i18n/locales/`
2. سجل اللغة في `src/i18n/index.ts`
3. أضف اللغة لقائمة `locales` في `middleware.ts`
4. أضف اللغة في `generateStaticParams()` في `app/[locale]/layout.tsx`

## 🐛 حل المشكلات الشائعة

### خطأ: "params is a Promise"
- **الحل**: استخدم `React.use()` لفتح params
- هذا إلزامي في Next.js 15+

### الصور لا تظهر
- **الحل**: تأكد من وجود الصور في مجلد `public/`
- استخدم المسار المباشر: `/image.png` بدون import

### اللغة لا تتغير
- **الحل**: تأكد من الذهاب للرابط الكامل مثل `/ar` أو `/en`
- زر تبديل اللغة يوجهك تلقائياً

